- Ruby 2.3.1 newest version
- 'type' will cause issues with ActiveRecord 
- 'yield' could cause issues?
- Struct (instead of OpenStruct) to be explicit about attributes for a 'Bond'. As well as ability to add convenience methods.
